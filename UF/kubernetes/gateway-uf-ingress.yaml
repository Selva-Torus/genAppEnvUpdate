apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: ct005-vgph-uf-generator-virtualservice
  namespace: ct005-vgph-uf-v1
spec:
  hosts:
    - "tgaprod910.toruslowcode.com"
  gateways:
    - istio-system/tgaprod-gw
  http:
    - match:
        - uri:
            prefix: /ct005/gss/vgph/v1
      route:
        - destination:
            host: ct005-vgph-uf-generator
            port:
              number: 443 
    - match:
        - uri:
            prefix: /ct005/gss/vgph/v1
      rewrite:
        uri: /
      route:
        - destination:
            host: ct005-vgph-uf-generator
            port:
              number: 443
 
 