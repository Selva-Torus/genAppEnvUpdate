apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: ct006-ams-uf-generator-virtualservice
  namespace: ct006-ams-uf-v1
spec:
  hosts:
    - "tgadev2.toruslowcode.com"
  gateways:
    - istio-system/wp-gateway
  http:
    - match:
        - uri:
            prefix: /ct006/ecp/ams/v1
      route:
        - destination:
            host: ct006-ams-uf-generator
            port:
              number: 443 
    - match:
        - uri:
            prefix: /ct006/ecp/ams/v1
      rewrite:
        uri: /
      route:
        - destination:
            host: ct006-ams-uf-generator
            port:
              number: 443
 
 